name: "123"

on:
  pull_request:
    branches: ['*']

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Get branch name
        id: get_branch_name
        uses: nelonoel/branch-name@v1.0.1
      - name: Build image
        run: docker build .
      
        
